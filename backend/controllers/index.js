const authController = require("./authController");
const productController = require("./productController");
const myStoreController = require("./myStoreController");
const myProductController = require("./myProductController");

module.exports = {
  authController,
  productController,
  myStoreController,
  myProductController,
};
